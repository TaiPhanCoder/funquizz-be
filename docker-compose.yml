version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: funquizz-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: funquizz
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - funquizz-network

  redis:
    image: redis:7-alpine
    container_name: funquizz-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    networks:
      - funquizz-network

  app:
    build: .
    container_name: funquizz-app
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USERNAME=postgres
      - DB_PASSWORD=postgres
      - DB_DATABASE=funquizz
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - postgres
      - redis
    networks:
      - funquizz-network

volumes:
  postgres_data:
  redis_data:

networks:
  funquizz-network:
    driver: bridge